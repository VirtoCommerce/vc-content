<div class="business-cnt mb-3">
    {% if block.title != '' %}
	    <div class="head">
		    <h1 class="title">{{block.title}}</h1>
	    </div>
    {% endif %}
    {% if block.subtitle != '' %}
        <div class="text">
            {{block.subtitle}}
        </div>
    {% endif %}
	<img alt="Virto Commerce - Glossary" src="assets/images/glossary.jpg" />
	<div class="buttons">
        {% include 'block-buttons' firstBtn: block.firstButton, secondBtn: block.secondButton %}
	</div>
</div>